-- Admin Seed: Create a super admin user
-- IMPORTANT: Replace the email and user_id below with your actual admin user credentials
-- First, create the user via Supabase Auth (signup), then insert here using their UUID

-- After you create your admin user via the auth system, run:
-- INSERT INTO public.admin_users (id, admin_role, display_name)
-- VALUES ('YOUR-USER-UUID-HERE', 'super_admin', 'Super Admin');

-- Example (replace with real values):
-- insert into public.admin_users (id, admin_role, display_name)
-- values ('00000000-0000-0000-0000-000000000000', 'super_admin', 'Platform Admin');
